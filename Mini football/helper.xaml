<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:av="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="av" x:Class="WASDControls.MainWindow"
        Title="WASD Controls" Height="302" Width="400">
    <Grid>
        <!-- Podešavamo mrežu sa 3 kolone i 3 reda za raspored dugmadi -->
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Dugme W - na sredini gornjeg reda -->
        <Button Grid.Row="0" Content="W" FontSize="24" Style="{StaticResource MenuButtonStyle}"
                Width="60" Height="60" HorizontalAlignment="Left" VerticalAlignment="Top"
                Margin="99,30,0,0" Grid.ColumnSpan="2" />

        <Button Grid.Row="1" Grid.Column="0" Content="A" FontSize="24" Style="{StaticResource MenuButtonStyle}"
                Width="60" Height="60" HorizontalAlignment="Left" VerticalAlignment="Top"
                Margin="40,0,0,0" />

        <Button Grid.Row="1" Content="S" FontSize="24"
                Width="60" Height="60" HorizontalAlignment="Left" VerticalAlignment="Top"
                Style="{StaticResource MenuButtonStyle}" Margin="105,0,0,0" Grid.ColumnSpan="2" />

        <Button Grid.Column="1" Content="D" FontSize="24" Style="{StaticResource MenuButtonStyle}"
                Width="60" Height="60" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="40,0,0,0" Grid.Row="1" />
     
        <Button Grid.Row="1" Grid.Column="2" Content="X" FontSize="24" Style="{StaticResource MenuButtonStyle}"
            Width="60" Height="60" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="23,0,0,0" />
    </Grid>
</Window>
